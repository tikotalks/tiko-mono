{
  "name": "@tiko/auth-middleware",
  "version": "0.1.0",
  "description": "Shared authentication middleware for Tiko workers — API key and session validation, rate limiting, and Pro tier entitlements",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "~5.3.3",
    "vitest": "^1.3.1"
  }
}
