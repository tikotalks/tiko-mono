{
  "name": "marketing",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "websites/marketing/src",
  "projectType": "application",
  "tags": ["scope:website", "type:app"],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/websites/marketing",
        "configFile": "websites/marketing/vite.config.ts"
      },
      "configurations": {
        "production": {
          "mode": "production"
        }
      },
      "dependsOn": ["^build"]
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "options": {
        "buildTarget": "marketing:build",
        "configFile": "websites/marketing/vite.config.ts",
        "port": 5173
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "options": {
        "buildTarget": "marketing:build",
        "configFile": "websites/marketing/vite.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["websites/marketing/**/*.{ts,tsx,js,jsx,vue}"]
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vue-tsc --noEmit",
        "cwd": "websites/marketing"
      }
    }
  }
}